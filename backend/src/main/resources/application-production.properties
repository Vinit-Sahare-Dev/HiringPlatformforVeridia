# Production Profile Configuration
# This configuration is used when SPRING_PROFILES_ACTIVE=production

# Server Configuration
server.port=${PORT:8081}

# Database Configuration - Production
# These values match the docker-compose.yml environment variables
spring.datasource.url=${DB_URL:jdbc:mysql://mysql:3306/veridia_hiring?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:newpassword}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration
jwt.secret=${JWT_SECRET:veridiaHiringPlatformSecretKey2024}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration - Production
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.protocol=smtp
spring.mail.username=${SPRING_MAIL_USERNAME:empsyncofficial@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:epegxhucplodalnu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Email settings
empsync.email.from=${SPRING_MAIL_FROM:empsyncofficial@gmail.com}
empsync.email.verification.enabled=true
empsync.email.verification.expiry-hours=24

# Logging Configuration - Production
logging.level.org.springframework.mail=WARN
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN
logging.level.com.veridia.hiring=INFO

# CORS Configuration
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:3000}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
